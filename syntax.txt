                МОДУЛЬ 2 "Основы JavaScript"


        Программы написаннные на JS называются скриптами.
        Не стоит путать Java и JavaScript.

        JS придаёт функционал (динамику) на Web страницах (например: Web игра)
        C помощью JS можно создавать динамические html и css элементы.

        alert('Hello, Poluhin!') Данная функция выводит какой-либо результат в виде всплывающего окна
        console.log('Hello, Poluhin!') Данная функция выводит какой либо результат в консоль

                        1 ТЕМА
    Переменные - Это ячейка памяти для хранения данных.

        Это операторы обьявлений переменных:

    let - это локальная переменная (любой тип данных)
    var - глобальная переменная 
    const - локальная константа (неизменяемая, непереопределяемая переменная)

Если не писать нигде переменные то по умолчанию все будут глобальные (let)
                           2 ТЕМА
Типы данных - 
a = 5 - Number - целочисленное и Int (число)
b = "Hello", - String (строка)
c = true/false  булеое значение (boolean)               
d = [3, "test", 5,5], массив данных (array)
c = { name: "Poluhin", age; 40}                       
e = a+a=10 объект                                                                                
f = b+b= ("Hello Hello")
g = a+b = "5Hello"

null- пусто, память выделена, но ничего нет
undefined- когда пытаешься вывести но не объявленна


                    ТЕМА 3
                ВСТРОЕННЫЕ ФУНКЦИ ВЗАИМОДЕЙСТВИЕ С ПОЛЬЗОВАТЕЛЕМ
alert()                
prompt() - Вызывает всплывающее окно в браузере для ввода данных с клавиатуры 
confirm() - Вызывает всплывающее окно в бразузере для потдверждения действий пользователей

userName=prompt()
alert("Привет, " + userName)
ТЕМА 4
Преобразование типов данных
JavaScript - автоматически выполняет преобразовние типов в различных контекстах, но так же он позволяет явно выполнить такие преобразования 
a = "123" (string)
b = 5
new A = Number (от а)
console.log (a + b)
a="123";  
b=5;    
newA=Number(а);
newB=String(b);
c="3,14";
newC=parseInt(c);
console.log(newC)
Number Явно пытается преобразовть переменную в числовой тип данных 
parseInt Преобразует в числовой тип данных и убирает дробную часть 
String преобразует любую переменную в строку

                обьекты 


  let user = new Object(); // синтаксис "конструктор объекта"
let user = {};  // синтаксис "литерал объекта"

При использовании литерального синтаксиса {...} мы сразу можем поместить в объект несколько свойств в виде пар «ключ: значение»:

let user = {     // объект
  name: "John",  // под ключом "name" хранится значение "John"
  age: 30        // под ключом "age" хранится значение 30
};

// получаем свойства объекта:
alert( user.name ); // John
alert( user.age ); // 30
Значение может быть любого типа. Давайте добавим свойство с логическим значением:

user.isAdmin = true;


Для удаления свойства мы можем использовать оператор delete:

delete user.age;

Объект, объявленный через const, может быть изменён.

Например:

const user = {
  name: "John"
};

user.name = "Pete"; // (*)

alert(user.name); // Pete

||||


let user = {};

// присваивание значения свойству
user["likes birds"] = true;

// получение значения свойства
alert(user["likes birds"]); // true

// удаление свойства
delete user["likes birds"];




let key = prompt("Что вы хотите узнать о пользователе?", "name");


let fruit = prompt("Какой фрукт купить?", "apple");

let bag = {
  [fruit]: 5, // имя свойства будет взято из переменной fruit
};

alert( bag.apple ); // 5, если fruit="apple"


alert( 'привет мир!!!');




// let message;
// message = 'hello world!!';  ///////////////    Let переменая для обозначения  слов и цыфр
// alert(message);
 



// let user = 'шнее пепе';
// age = '17';
// alert(user);
// alert(age);



 // console.log(1+4+3+6)

//  let user = {   
// name: "Daniil",
// age: "17"
//  };
//  alert( user.name );
//  alert( user.age );


// delete user.age;

// const user = {
//   name: "Daniil"
// };

// user.name = "Pete"; 

// alert(user.name);



Тема 5
Математичиские операции
 let x = 10;
 let y = 5;
 let z = "first";
 let h = "second";
 console.log (x+y); - сложение
 console.log (x-y); - минус
 console.log (x*y); - умножить
 console.log (x/y); - разделить
 console.log (x**y); -возвести в степень
 console.log (x%y); - проверка остатка


// let x;
// let y = 10;
// x=prompt("сколько вам лет")
// alert(x + y)

//  let x = Number(prompt("введите свой возраст"));
// alert('ваш возраст через 10 лет '+ (x+10));

let age = prompt( "сколько тебе лет?");
age = Number(age)
let newAge = age + 10;
alert( "вам лет:" + newAge)

Тема 6 
Операторы сравнения

< условие верно если 1 оперант меньше 2
2<5 true
> условие верное если 1 опернат больше 2
<= условие верно елси 1 оперант меньше или равен 2
>= условие верное если 1 опернат больше или равен 2
== условие верно если 1 оперант  равен 2 a == b true
=== условие верное если условие истина если оба оперната равны и имеют одинаковый тип данных  a === b false
!= условие показывает что  напривер x не равен 5   x != 5

Тема 7 
 условные оператооры
  (if, else)

  условные операторы позволяют выполнятьразличные действия зависимости выполнения  условий
  Основные условы if(), else{}, else if(){}.
  инструкция if(): вычисляет выражения в круглых скобках и приобразует к логическому типу 

  let x = 10;
if ( x > 5){    
    console.log('x больше 5')
}

let x = + prompt();

if ( x > 5){    
    console.log('x больше 5')
}
else if( x === 5) { 
    console.log( 'x = 5')
}
else{   
    console.log(' x < 5')
}
можно одни if но else обязателен елси условий не сколько ставим несколько 
применение терного оператора

тема 8 логически операторы
логические операторы используется для комбинирования  или изменения логических значений
простым словом они позволяют создать более  сложные условия для проверки
let x = true;
let y = false;
логичиска &&-и что такое логическая и это посути умножение то есть  x && y = false
второй логический оператор || логическая или по сути это сложение x || y = true


Домашние задание: повторить всё что изучили
1 задача напишите программу которая запрашивает у пользователя число с клавиатууры 
и проверяет прнедлежит ли число дипозону от 10 включительно и до 100 результат в консоль принадлежит или не принадлежит
2 задача напишите программу которая запрашивает у пользователя  число целое и выводит сообщение о том число 
чётное или нечётное
3 задача напишите программу которая проверяет возраст пользователяя введёного с клавиатуры если возраст 
пользователя меньше 18 лет то в консоль должно вывести доступ запрещён если старше то дсотуп разрешён задачу
задачу надо решить с преминением  тернарного оператора




ДОМАШНИЯ РАБОТА   
let number1 = prompt("напишите число с 10 до 100");
number1 = Number(number1);
 if ( number1 >= 10 && number1 <= 100) { 
     console.log( "ты молодец число входит в диапозон");
 }
 else {      
    console.log("число не входит в диапозон");
 }

  let number1 = prompt("напишите число для проверки чётное оно или нет")
number1 = Number(number1);
 if ( number1 % 2 === 0) { 
     console.log("число чётное")
 }
 else {  
     console.log ("число нечётное!")
 }